# ── Web framework ─────────────────────────────────────────────────────────
flask==3.1.0
flask-cors==5.0.0
gunicorn==23.0.0
# ── AI ────────────────────────────────────────────────────────────────────
# httpx MUST be pinned to <0.28.0. httpx 0.28.0 removed the `proxies=`
# parameter from Client.__init__(). The anthropic SDK passes proxies=
# internally — pinning 0.27.2 (last stable with proxies support) prevents
# the TypeError: Client.__init__() got an unexpected keyword argument 'proxies'
httpx==0.27.2
anthropic>=0.50.0
# ── PDF text extraction (digital PDFs) ────────────────────────────────────
PyMuPDF==1.24.14
pdfminer.six==20231228
# ── OCR (scanned / image-based PDFs) ─────────────────────────────────────
pdf2image==1.17.0
pytesseract==0.3.13
Pillow==11.0.0
# ── Word document extraction AND generation ───────────────────────────────
# (also used by build_cv_docx() in the ATS engine to write the output CV)
python-docx==1.1.2
